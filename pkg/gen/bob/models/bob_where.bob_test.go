// Code generated by BobGen sqlite v0.49.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"strings"
	"testing"

	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/dialect/sqlite"
	"github.com/stephenafamo/bob/dialect/sqlite/sm"
)

// TestDayHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestDayHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("DeepWorkLogs", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Days.NameExpr()),
			SelectWhere.Days.R.HasDeepWorkLogs(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasDeepWorkLogs: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasDeepWorkLogs: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("FitnessLogs", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Days.NameExpr()),
			SelectWhere.Days.R.HasFitnessLogs(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasFitnessLogs: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasFitnessLogs: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Habits", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Days.NameExpr()),
			SelectWhere.Days.R.HasHabits(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasHabits: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasHabits: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SleepLogs", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Days.NameExpr()),
			SelectWhere.Days.R.HasSleepLogs(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSleepLogs: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSleepLogs: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestDeepWorkLogHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestDeepWorkLogHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Day", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(DeepWorkLogs.NameExpr()),
			SelectWhere.DeepWorkLogs.R.HasDay(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasDay: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasDay: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestFitnessLogHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestFitnessLogHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Day", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(FitnessLogs.NameExpr()),
			SelectWhere.FitnessLogs.R.HasDay(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasDay: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasDay: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestHabitHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestHabitHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Day", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Habits.NameExpr()),
			SelectWhere.Habits.R.HasDay(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasDay: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasDay: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("HabitCategory", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Habits.NameExpr()),
			SelectWhere.Habits.R.HasHabitCategory(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasHabitCategory: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasHabitCategory: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestHabitCategoryHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestHabitCategoryHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Habits", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(HabitCategories.NameExpr()),
			SelectWhere.HabitCategories.R.HasHabits(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasHabits: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasHabits: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestSleepLogHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestSleepLogHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Day", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(SleepLogs.NameExpr()),
			SelectWhere.SleepLogs.R.HasDay(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasDay: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasDay: expected EXISTS in query, got: %s", sql)
		}
	})
}
